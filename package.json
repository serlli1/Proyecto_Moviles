{
  "name": "telegram-http-proxy",
  "version": "1.0.0",
  "description": "Proxy HTTP para SIM800C que redirige mensajes a Telegram vía HTTPS",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "express": "^4.18.2"
  }
}
